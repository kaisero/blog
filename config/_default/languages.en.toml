disabled = false
locale = "en"
label = "English"
weight = 1
title = "dependencyhell.com"

[params]
  displayName = "EN"
  isoCode = "en"
  rtl = false
  dateFormat = "2 January 2006"  # design system: "11 August 2026"
  description = "Notes on AI security, Zero Trust and SASE — from the lab, not the datasheet."
  # Shown in the homepage hero under the wordmark. The hero lockup is sized to
  # match this line's width, so re-check the alignment if you change it.
  tagline = "Trust Nothing. Verify Everything. Hallucinate the Rest."
  copyright = "© 2026 dependencyhell.com"

[params.author]
  name = "Oliver Mackenzie"
  # Resolved from assets/. SVG is passed through untouched — the theme only runs
  # Fill/Crop on raster images. Swap in a photo when you have one.
  image = "img/avatar-placeholder.svg"
  headline = "Domain Consultant at Palo Alto Networks"
  bio = "I work on Zero Trust and SASE architectures. This is where I write up what I actually see when I put the products on a bench — the failure modes, the defaults nobody documents, and where AI is quietly changing the threat model. Opinions are mine."
  links = [
    { email = "mailto:oliver@mackenzie.co.at" },
    { linkedin = "https://linkedin.com/in/kaisero" },
    { github = "https://github.com/kaisero" },
  ]
